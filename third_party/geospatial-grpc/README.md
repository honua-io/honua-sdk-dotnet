# geospatial-grpc

This directory contains vendored `.proto` inputs from the `geospatial-grpc`
repository. That repository is the source of truth for these contracts; this
SDK keeps only the proto files required to generate its .NET gRPC client.

Do not edit these files directly in this repository. Update them from
`geospatial-grpc` when the upstream contract changes.
